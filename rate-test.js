const URL_BASE = process.argv[2] || "https://consumable-api-alpha.vercel.app"; const TOTAL = 105; const BATCH_SIZE = 3; const DELAY_MS = 100; function sleep(ms) { return new Promise((resolve) => setTimeout(resolve, ms)); } async function fireOne(i) { try { const res = await fetch(`${URL_BASE}/api/v1/categories`); console.log(`Attempt ${i}: ${res.status}`); } catch (err) { console.log(`Attempt ${i}: FAILED (${err.message})`); } } async function main() { for (let start = 1; start <= TOTAL; start += BATCH_SIZE) { const batch = []; for (let i = start; i < start + BATCH_SIZE && i <= TOTAL; i++) { batch.push(fireOne(i)); } await Promise.all(batch); await sleep(DELAY_MS); } } main();